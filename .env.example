# 服务监听环境
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# PostgreSQL 示例连接；请按本地环境替换
DATABASE_URL=postgresql://docker_snake:change-me@localhost:5432/docker_snake

# 必须自行生成至少 32 字节的随机值，禁止使用公开固定字符串
SESSION_SECRET=

# 浏览器访问源与反向代理层数
PUBLIC_ORIGIN=http://localhost:3000
TRUST_PROXY=0
